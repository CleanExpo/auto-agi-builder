# Environment Variables for Vercel Deployment
# Replace the values with the actual credentials from your Vercel project
# Then rename this file to .env.local for local development or use Vercel's UI to set these in production

# Vercel Project Details
VERCEL_TOKEN="your_vercel_api_token"
VERCEL_ORG_ID="your_vercel_org_id"  
VERCEL_PROJECT_ID="your_vercel_project_id"

# API Configuration
NEXT_PUBLIC_API_URL="https://your-api-endpoint.com"

# Authentication
NEXT_PUBLIC_AUTH_DOMAIN="your-auth-domain"
NEXT_PUBLIC_AUTH_CLIENT_ID="your-auth-client-id"

# Other Configuration
NEXT_PUBLIC_ENVIRONMENT="production"  # or "development", "staging", etc.
